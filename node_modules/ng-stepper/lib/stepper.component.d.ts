import { EventEmitter, TemplateRef } from '@angular/core';
import { Step } from './step.model';
export declare class StepperComponent {
    stepTitleTemplateRef: TemplateRef<void>;
    steps: Step[];
    dotSize: number;
    lineWidth: number;
    defaultDotColor: string;
    defaultDashColor: string;
    onDotClick: EventEmitter<Step>;
    docClicked(step: any): void;
    dotWidthAndHeight(): string;
    dashTopPosition(): string;
    lineWidthInPixels(): string;
}
